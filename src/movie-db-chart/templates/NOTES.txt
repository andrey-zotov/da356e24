Run the command, open locust and enter the URLs to stress test:
  echo http://$(kubectl get services --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "movie-db-chart.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].spec.clusterIP}"):$(kubectl get services --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "movie-db-chart.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].spec.ports[0].port}")
  echo http://$(kubectl get services --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "movie-db-chart.name" . }}-rs,app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].spec.clusterIP}"):$(kubectl get services --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "movie-db-chart.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].spec.ports[0].port}")
